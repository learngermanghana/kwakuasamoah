# Workspace Specific Agent Rules

- **Framework**: Use Next.js 16 (App Router) and React 19.
- **Styling**: Use Tailwind CSS v4.
- **Integration**: All booking flows must integrate through the `src/app/api/integration-bookings` route to protect API keys.
- **Testing**: Run tests using `npm test` after modifying core utility files in `src/lib/`.
